# 🏷 Механика стикеров (Decals)

Система позволяет пользователю кликнуть в любую точку 3D-модели и "приклеить" туда знак, который корректно ляжет на поверхность (даже если она круглая).

## ⚙️ Как это работает

Логика реализована в `js/interactions/InteractionManager.js`.

### 1. Генерация текстур на лету
Мы не храним тысячи картинок. Текстуры создаются программно:
1.  Создается HTML5 `<canvas>`.
2.  На нем рисуется эмодзи или текст (из атрибута `data-sticker` в HTML).
3.  Canvas превращается в `THREE.CanvasTexture`.

### 2. Raycasting (Поиск точки)
Когда пользователь водит мышкой:
1.  Из камеры пускается невидимый луч (`THREE.Raycaster`).
2.  Находится точка пересечения с моделью (`point`) и нормаль полигона (`face.normal`).
3.  Отображается полупрозрачный "призрак" стикера (Preview).

### 3. Проекция (DecalGeometry)
При клике создается **Decal**:
*   Это специальная геометрия Three.js, которая "вырезает" кусок меша модели в месте клика и проецирует на него текстуру.
*   Используется `polygonOffset`, чтобы стикер не мерцал и был всегда чуть выше поверхности модели.

## 💻 Как добавить новый стикер

Вам не нужно писать код. Просто отредактируйте `index.html`:

1.  Найдите блок `<div id="sticker-drawer">`.
2.  Добавьте новый элемент:

```html
<div class="sticker-option" data-sticker="☢️">
    <span class="char-sticker">☢️</span>
    <label>Radioactive</label>
</div>
```

Система сама сгенерирует текстуру из символа "☢️" и добавит его в логику.