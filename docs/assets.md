# 📦 Работа с 3D моделями

Система умеет загружать произвольные модели транспорта и автоматически подготавливать их к работе.

## Поддерживаемые форматы
*   **.glb / .gltf** (Рекомендуется) — сохраняют иерархию и материалы.
*   **.stl** — только геометрия (будет покрашена в серый цвет).

## 🎨 Автоматическая обработка (`AssetLoader.js`)

При загрузке любой модели скрипт выполняет следующие действия:

1.  **Нормализация размера:** Модель масштабируется так, чтобы помещаться в камеру (приводится к размеру ~4 условных единиц).
2.  **Центрирование:** Модель сдвигается в центр координат `(0,0,0)`.
3.  **Покраска (Car Paint):** Оригинальные материалы заменяются на шейдер "Автомобильная краска" (MeshPhysicalMaterial) с настройками:
    *   `metalness: 0.7`
    *   `roughness: 0.4`
    *   `clearcoat: 1.0` (эффект лака)

## ➕ Как добавить новую машину

1.  Поместите файл `.glb` в корневую папку проекта (рядом с `index.html`).
2.  Откройте `js/io/AssetLoader.js`.
3.  Найдите функцию `loadDefaultModel()` и измените имя файла:

```javascript
// Было
new GLTFLoader().load('base (4).glb', ...);

// Стало
new GLTFLoader().load('my_new_truck.glb', ...);
```

## 🔗 Привязка зон к модели

Система ищет файл конфигурации по имени модели.
Если ваша модель называется `tanker.glb`, то файл с зонами должен называться `zones_tanker_glb.json`.